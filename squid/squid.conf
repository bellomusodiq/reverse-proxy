http_port 3128

# Allow all clients (development only!)
acl all src all
http_access allow all

# Use default logging paths
# (e.g., /var/log/squid/access.log, /var/log/squid/cache.log)

# DNS
dns_nameservers 8.8.8.8 1.1.1.1

# Disable caching
cache deny all
